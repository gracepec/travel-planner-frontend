export interface FlightType {
    airline: string;
    airline_logo: string;
    dpt_dpt_time: string;
    dpt_dpt_apt: string;
    dpt_arv_time: string;
    dpt_arv_apt: string;
    dpt_time: string;
    rtn_dpt_time: string;
    rtn_dpt_apt: string;
    rtn_arv_time: string;
    rtn_arv_apt: string;
    rtn_time: string;
    price: string;
    url: string;
}
